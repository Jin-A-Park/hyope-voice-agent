# session(instructions)/systemInstruction 전체를 없앴다가, 모델이 통화 시작하자마자 툴 호출도
# 없이 자기 기본 정체성("Eve")으로 영어 인사부터 반사적으로 뱉는 문제가 발견됐다 — 대화 중간에
# 아이템 하나 찔러넣는 방식(가짜 user/system 턴)으로는 못 막았다. 모델의 기본 정체성 자체를
# 덮어쓰는 건 세션 레벨 지시만 할 수 있는 역할이라, 그 용도로만 쓰이는 한 줄짜리 최소 지시문을
# 다시 둔다(예전의 여러 문단짜리 SYSTEM_PROMPT는 부활시키지 않는다 — 나머지 규칙은 이미
# next_step/GREETING_PROMPT로 옮겨져 있다).
IDENTITY_PROMPT = (
    "너는 한국 독거노인분들의 건강 체크를 위해 전화하는 \"어르신 건강관리 상담원\"이다. 항상 "
    "한국어로만 답하고, 통화가 시작되면 다른 말 없이 곧바로 통화 시작하는 함수부터 호출하라."
)

# 고정 문장이 아니라 인사말이 지켜야 할 규칙 모음이다 — 모델이 매번 이 규칙을 바탕으로 자기 말투로
# 직접 인사말을 지어낸다. 인사말 하나 건네는 것뿐이라 스테이지 상태머신(레벨/판정)까지 태울 필요가
# 없어서 GREETING은 StageType이 아니지만, start_call 툴 호출 자체는 남겨서 이 규칙을 그 반환값에
# 실어 보낸다(agent/tools.py의 start_call 참고).
GREETING_PROMPT = (
    "너는 한국 독거노인분들의 건강 체크를 위해 전화 건 \"어르신 건강관리 에이전트\"다. 인사 및 어르신의 안부를 친근하게 한국어로 여쭤봐라"
)
